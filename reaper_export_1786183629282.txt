-- Credit : Reaper Hub
-- REAPER HUB (SUPER STABLE VERSION)
repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local DrawingLib = (type(Drawing) ~= "nil")

--------------------------------------------------
-- 1. SETTINGS & STATES
--------------------------------------------------
local States = {
    Aimbot = false, LockLevel = 1, TargetPart = "Head", UseFOV = true, FOVRadius = 150,
    HitboxEnabled = false, HitboxSize = 10, HitboxTransparency = 0.7,
    ESP_Highlight = false, ESP_Tracers = false, ESP_NameTags = false
}
local SmoothnessMap = {[1] = 0.15, [2] = 0.40, [3] = 1.00}
local ESP_Players = {}

--------------------------------------------------
-- 2. UI CONSTRUCTION (เปลี่ยนไปใช้ CoreGui)
--------------------------------------------------
-- ลบของเก่าออกก่อนรันใหม่
if CoreGui:FindFirstChild("ReaperProject_Final") then
    CoreGui:FindFirstChild("ReaperProject_Final"):Destroy()
end

local gui = Instance.new("ScreenGui")
gui.Name = "ReaperProject_Final"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
gui.Parent = CoreGui -- เปลี่ยนจาก PlayerGui เป็น CoreGui

local main = Instance.new("Frame", gui)
main.Size = UDim2.fromOffset(550, 350)
main.Position = UDim2.new(0.5, -275, 0.5, -175)
main.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true 

Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)

-- Sidebar & Content
local sidebar = Instance.new("Frame", main)
sidebar.Size = UDim2.new(0, 130, 1, -50)
sidebar.Position = UDim2.fromOffset(10, 40)
sidebar.BackgroundColor3 = Color3.fromRGB(22, 22, 26)
Instance.new("UICorner", sidebar)

local content = Instance.new("Frame", main)
content.Size = UDim2.new(1, -160, 1, -50)
content.Position = UDim2.fromOffset(150, 40)
content.BackgroundColor3 = Color3.fromRGB(22, 22, 26)
Instance.new("UICorner", content)

-- FOV Circle (Using Frame for compatibility)
local fovCircle = Instance.new("Frame", gui)
fovCircle.Size = UDim2.fromOffset(States.FOVRadius*2, States.FOVRadius*2)
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.BackgroundTransparency = 1
fovCircle.Visible = false
local fovStroke = Instance.new("UIStroke", fovCircle)
fovStroke.Color = Color3.fromRGB(255, 50, 50)
fovStroke.Thickness = 1
Instance.new("UICorner", fovCircle).CornerRadius = UDim.new(1, 0)

--------------------------------------------------
-- 3. LOGIC FUNCTIONS
--------------------------------------------------
local function GetClosestTarget()
    local closest, shortestDist = nil, math.huge
    local mousePos = UserInputService:GetMouseLocation()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild(States.TargetPart) then
            local pos, onScreen = Camera:WorldToViewportPoint(p.Character[States.TargetPart].Position)
            if onScreen then
                local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                if dist < (States.UseFOV and States.FOVRadius or 1000) and dist < shortestDist then
                    shortestDist, closest = dist, p
                end
            end
        end
    end
    return closest
end

local function UpdateHitboxes()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local root = p.Character:FindFirstChild("HumanoidRootPart")
            if root then
                if States.HitboxEnabled then
                    root.Size = Vector3.new(States.HitboxSize, States.HitboxSize, States.HitboxSize)
                    root.Transparency = States.HitboxTransparency
                    root.Color = Color3.fromRGB(255, 0, 0)
                else
                    root.Size = Vector3.new(2, 2, 1)
                    root.Transparency = 1
                end
            end
        end
    end
end

--------------------------------------------------
-- 4. UI BUILDER
--------------------------------------------------
local panels = {}
local function createTab(name, id)
    local p = Instance.new("ScrollingFrame", content)
    p.Size = UDim2.new(1, -10, 1, -10)
    p.Position = UDim2.fromOffset(5, 5)
    p.BackgroundTransparency = 1
    p.Visible = false
    p.CanvasSize = UDim2.new(0,0,2,0)
    p.ScrollBarThickness = 2
    Instance.new("UIListLayout", p).Padding = UDim.new(0, 5)
    panels[id] = p
    
    local b = Instance.new("TextButton", sidebar)
    b.Size = UDim2.new(1, -10, 0, 35)
    b.Position = UDim2.fromOffset(5,0)
    b.Text = name
    b.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    b.TextColor3 = Color3.white
    b.Font = Enum.Font.Code
    Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function()
        for _, v in pairs(panels) do v.Visible = false end
        p.Visible = true
    end)
end

local function createToggle(panel, text, callback)
    local state = false
    local b = Instance.new("TextButton", panel)
    b.Size = UDim2.new(1, 0, 0, 35)
    b.Text = text .. " : OFF"
    b.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    b.TextColor3 = Color3.white
    b.Font = Enum.Font.Code
    Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function()
        state = not state
        b.Text = text .. " : " .. (state and "ON" or "OFF")
        b.BackgroundColor3 = state and Color3.fromRGB(200, 50, 50) or Color3.fromRGB(40, 40, 45)
        callback(state)
    end)
end

createTab("🎯 AIMBOT", "aim")
createTab("📦 HITBOX", "hb")
createTab("👁 VISUALS", "vis")
panels.aim.Visible = true

createToggle(panels.aim, "Aimbot", function(s) States.Aimbot = s fovCircle.Visible = s end)
createToggle(panels.hb, "Hitbox Expander", function(s) States.HitboxEnabled = s end)
createToggle(panels.vis, "Show FOV Circle", function(s) States.UseFOV = s fovCircle.Visible = States.Aimbot and s end)

--------------------------------------------------
-- 5. LOOPS
--------------------------------------------------
RunService.RenderStepped:Connect(function()
    if States.Aimbot then
        local t = GetClosestTarget()
        if t and t.Character then
            local targetPos = t.Character[States.TargetPart].Position
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, targetPos), SmoothnessMap[States.LockLevel])
        end
    end
    if fovCircle.Visible then
        local m = UserInputService:GetMouseLocation()
        fovCircle.Position = UDim2.fromOffset(m.X, m.Y)
    end
end)

RunService.Heartbeat:Connect(function()
    if States.HitboxEnabled then pcall(UpdateHitboxes) end
end)

-- Toggle Menu Button
local toggleBtn = Instance.new("TextButton", gui)
toggleBtn.Size = UDim2.fromOffset(45, 45)
toggleBtn.Position = UDim2.new(0, 10, 0.5, 0)
toggleBtn.Text = "R"
toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
toggleBtn.TextColor3 = Color3.white
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1, 0)
toggleBtn.MouseButton1Click:Connect(function() main.Visible = not main.Visible end)
